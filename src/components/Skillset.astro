---
import type { IconType } from 'react-icons';

export type Skill = {
  name: string;
  icon: IconType;
};

interface Props {
  skills: Skill[];
  className?: string;
}

const { skills, className = '' } = Astro.props;
---

<section
  class={`relative mt-[55px] pt-[35px] p-6 sm:px-8 border-[1.5px] border-cyan-300 rounded-[10px] grid gap-8 lg:gap-4 justify-items-center hidden-element ${className}`}
>
  <header
    class='absolute -top-4 left-[30px] px-[10px] border-[1.5px] border-y-0 border-x-cyan-300 bg-[#090f3f]'
  >
    <h4 class='text-xl'><slot /></h4>
  </header>

  {
    skills.map(async ({ icon, name }) => {
      const IconComponent = icon as IconType;

      return <IconComponent size={48} aria-label={name} />;
    })
  }
</section>
